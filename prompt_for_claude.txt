# Arbor Architecture & Codebase Review Prompt

Please perform a comprehensive codebase review of the Arbor project. I want to identify the best ways to improve the program, making it more secure, consistent, and performant.

## Instructions
1. **Read the Rules First:** Before analyzing the code, carefully review the four guide documents provided in the context:
   - `documentation/agent/CODE_REVIEW.md`
   - `documentation/agent/CLAUDE_BUG_REVIEW_GUIDE.md`
   - `documentation/agent/MOBILE_COMPANION_GUIDE.md`
   - `documentation/agent/AI_UI_GUIDE.md`
2. **Review Recent Changes:** Pay special attention to the recent architectural shifts introduced in the last 48 hours. Specifically, evaluate:
   - The massive monolithic file `ui/main_window.py` (7300+ lines) vs. the newly decoupled components in `ui/unified_settings.py`.
   - The implementation and usage of `EventBus` (`ui/state.py`) for decoupled state management and safe cross-thread UI updates.
   - The use of `BackgroundWorker` (`backend/task_queue.py`) for offloading heavy tasks from the Tkinter main thread.
   - The massive `backend/mobile_server.py` implementation, ensuring it strictly adheres to single-source-of-truth frontend (vanilla JS/HTML inside `INDEX_TEMPLATE`) and hybrid auto-save guidelines.
3. **Analyze Security & Consistency:**
   - Review how background threads (like `MobileServer` or tasks in `task_queue.py`) synchronize with the main Tkinter UI. Are there potential race conditions or direct UI calls that bypass `EventBus` or `root.after()`?
   - Review data handling for security (e.g., preventing formula injection during exports, safe serialization).
4. **Deliverables:**
   - **Codebase Recommendations:** Suggest 3 to 5 high-impact, specific improvements to the codebase that align with the new event-driven, decoupled architecture. Do not write the code for these changes; just explain the problem and the proposed architectural solution.
   - **Guide Evaluation:** Review the four guide documents themselves. Do they accurately and comprehensively capture the constraints of the new architecture? What critical rules or best practices are missing from these guides that should be added to prevent future AI agents from making mistakes?
